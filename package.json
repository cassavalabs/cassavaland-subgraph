{
  "name": "subgraph",
  "version": "1.0.0",
  "main": "index.js",
  "author": "cassava community",
  "license": "MIT",
  "scripts": {
    "build": "graph build",
    "codegen": "graph codegen",
    "deploy": "graph deploy --product hosted-service iphyman/cassavaland-moonriver",
    "create": "graph create cassavaland-subgraph --node https://api.thegraph.com/deploy/",
    "create-local": "graph create cassavaland-subgraph --node http://127.0.0.1:8020",
    "deploy-local": "graph deploy cassavaland-subgraph --ipfs http://localhost:5001 --node http://127.0.0.1:8020"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.30.0",
    "@graphprotocol/graph-ts": "^0.27.0",
    "typescript": "^4.6.4"
  }
}